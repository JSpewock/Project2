<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Index</title>
</head>
<body>
    <!-- loop to make 12 divs for the 12 weeks -->
        <% for (let i = 1; i <= 12; i++) {%>
            <div>
                <h2>Week <%=i%></h2>
                <ul>
                    <!-- loop to make an li for each day -->
                    <% for (let j = 0; j < allDays.length; j++) {%>
                        <% if (allDays[j].week === i) {%>
                            <li><%=allDays[j].name%></li>
                        <%}%>
                    <%}%>
                </ul>
            </div>
        <%}%>
</body>
</html>